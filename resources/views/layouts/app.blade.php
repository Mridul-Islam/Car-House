<!-- Header -->
@include('includes.header')

    <!-- Navigation -->
    @include('includes.navigation')

        <main>
            @yield('content')
        </main>

<!-- Footer -->
@include('includes.footer')
